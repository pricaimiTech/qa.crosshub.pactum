# Ambiente: develop — ambiente compartilhado de desenvolvimento/staging.
# Copiar para ".env.develop" e preencher os valores.

# URL base da API do CrossHub
BASE_URL=https://develop-api.crosshub.example.com

# Credenciais usadas no preSetup (login que gera os tokens dos testes)
ADMIN_EMAIL=
ADMIN_PASSWORD=
TENANT_SLUG=

# Liga/desliga o log das requisições Pactum
LOG=false
