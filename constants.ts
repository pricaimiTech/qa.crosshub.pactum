/**
 * Ponto de entrada de constantes da raiz (`@app/constants`).
 * A implementação vive no core para que services e utils possam
 * importá-la por caminho relativo dentro de `core/src`.
 */
export * from "./core/src/constants"
